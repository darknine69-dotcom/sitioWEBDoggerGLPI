DJANGO_SECRET_KEY=cambia-esta-clave-usa-openssl-rand-hex-32
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# --- Database ---
# SQL Server (local)
DB_NAME=Tickets
DB_USER=DGT
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=1433
DB_DRIVER=ODBC Driver 18 for SQL Server
DB_EXTRA_PARAMS=TrustServerCertificate=yes

# PostgreSQL (cloud) — Render crea DATABASE_URL automáticamente
# DATABASE_URL=postgres://user:pass@host:5432/dbname

# --- Dogger ---
DOGGER_CORREO=sistemasbogota@dogger.com.co
DOGGER_WHATSAPP=3103716129

# --- GLPI (solo funciona en LAN, desactivado en cloud) ---
GLPI_ENABLED=False
GLPI_BASE_URL=http://glpi.dogger.local/apirest.php
GLPI_APP_TOKEN=
GLPI_USER_TOKEN=
GLPI_TIMEOUT=30
GLPI_WEBHOOK_SECRET=
