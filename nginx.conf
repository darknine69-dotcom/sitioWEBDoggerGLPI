# Dogger HelpDesk — Nginx
# Sirve estáticos/media directamente y hace proxy a Gunicorn (web:8000).

# --- HTTP: redirigir a HTTPS (comentar si solo quieres HTTP interno) ---
server {
    listen 80;
    server_name _;

    # Para HTTPS con Certbot: descomenta esta redirección.
    # location / { return 301 https://$host$request_uri; }
}

# --- HTTPS (opcional) ---
# server {
#     listen 443 ssl;
#     server_name tu-dominio.com;
#
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#
#     location /static/ {
#         alias /app/staticfiles/;
#         expires 30d;
#     }
#
#     location /media/ {
#         alias /app/media/;
#     }
#
#     location / {
#         proxy_pass http://web:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

# --- HTTP directo (sin TLS aún) ---
server {
    listen 80;
    server_name _;

    client_max_body_size 12m;

    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
    }

    location /media/ {
        alias /app/media/;
    }

    location / {
        proxy_pass http://web:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
